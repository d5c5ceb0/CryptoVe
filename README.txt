for linux
1. build miracl
acording miracl/readme.txt
example: in linux64, run "bash linux64"
2. install tcl8.5-dev
3. copy miracl.a miracl.h mirdef.h to c_extend,make
4. cp libcrypto.so to cryptove/lib
5. run tclsh, and "source cryptove.tcl"

for windows
